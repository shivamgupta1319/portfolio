/**
 * Quest ids that have a screenshot at /public/shots/<id>.webp (1280×800, q80).
 * Generated/updated by `npm run shots` (scripts/capture-shots.mjs); add an id
 * here after the file lands so the card picks it up.
 */
export const SHOTS = new Set<string>([]);
